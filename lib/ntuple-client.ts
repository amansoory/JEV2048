import {legalMoves,type Direction} from '@/public/engine';
export async function ntupleDecision(board:number[],context?:{match:string;generation:number;sequence:number}){const response=await fetch('/api/ntuple',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({board,...context}),signal:AbortSignal.timeout(15000)});const result=await response.json();if(!response.ok)throw Object.assign(Error(result.error||'N-tuple is unavailable.'),{code:result.code});if(context&&(result.generation!==context.generation||result.sequence!==context.sequence))throw Error('Stale model response rejected.');if(!legalMoves(board).includes(result.direction)||!Number.isFinite(result.ms))throw Error('N-tuple returned an invalid decision.');return result as {direction:Direction;ms:number;computeMs:number;model:string;evidence:unknown};}
