import {evidenceTerms,presentTerms,outputTerms} from '@/lib/evidence-glossary';
export default function EvidenceGuide({evidence,all=false}:{evidence?:unknown;all?:boolean}){const terms=all?evidenceTerms:presentTerms(evidence);return <details className="evidence-guide"><summary>What the data means</summary><p>Jev receives the current board, legal anonymous outcomes, rules and the goal. Only the selected version adds the features below. It does not receive direction mappings, expert recommendations, timings, token counts, previous scores, the seed or the actual next spawn.</p><h3>{all?'Data that can be supplied to Jev':'Fields present in this move'}</h3><dl>{terms.map(t=><div key={t.name}><dt>{t.name}</dt><dd>{t.meaning}</dd></div>)}</dl><h3>Recorded after the choice, not sent to Jev</h3><dl>{outputTerms.map(([name,meaning])=><div key={name}><dt>{name}</dt><dd>{meaning}</dd></div>)}</dl></details>;}
