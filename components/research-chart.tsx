'use client';
import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip,CartesianGrid,Legend} from 'recharts';
export default function ResearchChart({data,series,label,log=false}:{data:Record<string,string|number>[];series:{key:string;name:string;color:string}[];label:string;log?:boolean}){return <div className="research-chart"><div style={{height:290,width:'100%',minWidth:0}}><ResponsiveContainer width="100%" height="100%" minWidth={0}><BarChart data={data} margin={{top:10,right:10,bottom:15,left:5}} accessibilityLayer><CartesianGrid vertical={false} stroke="#dde5e9"/><XAxis dataKey="label" tick={{fontSize:11}} interval={0}/><YAxis width={65} scale={log?'log':'auto'} domain={log?[.001,100]:[0,'auto']} ticks={log?[.001,.01,.1,1,10,100]:undefined} tick={{fontSize:11}} tickFormatter={v=>Number(v)>=10000?(Number(v)/1000)+'k':String(v)}/><Tooltip formatter={(v:any,n:any)=>[Number(v).toLocaleString(undefined,{maximumFractionDigits:4}),n]} contentStyle={{fontSize:13}}/><Legend wrapperStyle={{fontSize:12}}/>{series.map(s=><Bar key={s.key} dataKey={s.key} name={s.name} fill={s.color} radius={[4,4,0,0]} isAnimationActive={false}/>)}</BarChart></ResponsiveContainer></div><p>{label}</p><details><summary>View chart values</summary><div className="lab-table-scroll"><table><caption>{label}</caption><thead><tr><th>Observation</th>{series.map(s=><th key={s.key}>{s.name}</th>)}</tr></thead><tbody>{data.map((r,i)=><tr key={i}><th>{r.label}</th>{series.map(s=><td key={s.key}>{Number(r[s.key]).toLocaleString(undefined,{maximumFractionDigits:6})}</td>)}</tr>)}</tbody></table></div></details></div>;}
