import test from 'node:test';import assert from 'node:assert/strict';
import {PLAY_BOTS,PLAY_OPPONENTS,WATCH_PRESETS,botName,statusText} from '../lib/play-options';
import {POLICY_LIST} from '../public/policy-catalog';
test('You vs Jev has exactly the three requested opponents',()=>{assert.deepEqual(PLAY_OPPONENTS.map(b=>b.name),['Jev','Jev + board analysis','Jev + both experts']);assert.deepEqual(PLAY_OPPONENTS.map(b=>b.id),['blind-board','blind-feature','blind-dual']);});
test('Watch presets preserve all five Jev inputs and both specialist baselines',()=>{assert.deepEqual(WATCH_PRESETS.inputs.bots,['blind-board','blind-feature','blind-expectimax','blind-ntuple','blind-dual']);assert.deepEqual(WATCH_PRESETS.specialists.bots,['search','ntuple','blind-dual']);for(const preset of Object.values(WATCH_PRESETS)){assert.ok(preset.bots.length<=5);assert.equal(new Set(preset.bots).size,preset.bots.length);for(const id of preset.bots)assert.ok(PLAY_BOTS.some(b=>b.id===id));}});
test('Custom contains seven current bots and excludes historical variants without deleting them',()=>{assert.equal(PLAY_BOTS.length,7);assert.deepEqual(PLAY_BOTS.map(b=>b.name),['Jev','Jev + board analysis','Jev + expectimax','Jev + n-tuple','Jev + both experts','Expectimax','N-tuple']);for(const id of ['blind-strategy','raw','strategy','feature','assisted','heuristic']){assert.ok(POLICY_LIST.some(p=>p.id===id));assert.ok(!PLAY_BOTS.some(b=>String(b.id)===id));}for(const b of PLAY_BOTS)assert.ok(POLICY_LIST.some(p=>p.id===b.id));});
test('board names and statuses use the visible copy without changing internal IDs',()=>{assert.equal(botName('blind-dual'),'Jev + both experts');assert.equal(botName('human'),'You');assert.equal(statusText('waiting for Jev'),'Waiting for Jev');assert.equal(statusText('waiting for N-tuple'),'Loading n-tuple');assert.equal(statusText('policy failure'),'Request failed');assert.equal(statusText('game over'),'Game over');});
